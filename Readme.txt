An HTML file is provided along with the python Code. HTML file alone can be opened in any browser to see a fully functional visualization. This HTML file have been automatically created by Python code.

If you want to see the visualization from python code follow below instructions:


Requirements:
1: Python 3 should be Installed in the system

To run the project please follow below steps:
1: run below command
    pip install requirements.txt  ( This will install all the dependencies)
	
2: Once the dependencies are installed run below command:
	python Prem_Keshari_18303567.py
	
	this will open a window in the browser with all the visualization
